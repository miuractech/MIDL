rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /roles/{email} {
      allow read, write, update: if request.auth != null && request.auth.token.role == "admin";
    }
  }
}